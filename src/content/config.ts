import { defineCollection, z } from "astro:content";

export const collections = {
  /*  legalInfo: defineCollection({
    schema: z.object({
      title: z.string(),
      text: z.string(),
      slug: z.string(),
    }),
  }), */
};
