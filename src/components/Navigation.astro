---
const navItems = [
  { path: "/", label: "Home" },
  { path: "/login", label: "Login" },
  { path: "/dashboard", label: "Dashboard" },
];
const currentPath = Astro.url.pathname;
---

<nav class="sticky top-0 z-50 bg-white shadow-md">
  <div class="mx-auto flex max-w-5xl items-center justify-between px-4 py-3">
    <h1 class="text-xl font-semibold text-gray-900 tracking-tight">MySite</h1>

    <div class="flex gap-2 sm:gap-4">
      {
        navItems.map((item) => (
          <a
            href={item.path}
            class={`relative rounded-lg px-3 py-2 text-sm font-medium transition-colors duration-200
    ${
      currentPath === item.path
        ? "text-blue-600 after:content-[''] after:absolute after:bottom-0 after:left-0 after:h-[2px] after:w-full after:bg-blue-600"
        : "text-gray-700 hover:text-blue-600 hover:bg-blue-50"
    }`}
          >
            {item.label}
          </a>
        ))
      }
    </div>
  </div>
</nav>
